import React, { Component } from 'react'
import config from '../config'
import ApiContext from '../ApiContext'
import ErrorBoundary from '../ErrorBoundary'

export default class AddBoard extends React{
    constructor(props) {
        super(props);
        this.state = {
            title: {
                value: '',
                touched: false
            },
            board
            categories: [
                values: ''
            ],
            questions: [
                value:
            ]
        }
    }
}